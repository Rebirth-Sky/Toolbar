define(['jquery'], function($) {
    return {
        isEmpty: function() {
            // body...
        },
        checkLength: function(argument) {
            // body...
        },
        isEqual: function(str1, str2) {
            return str1 === str2;
        }
    }
})